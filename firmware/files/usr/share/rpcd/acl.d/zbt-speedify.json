{
  "zbt-speedify": {
    "description": "Control Speedify and read router sign-in status",
    "read": { "ubus": { "zbt.speedify": [ "status" ] } },
    "write": { "ubus": { "zbt.speedify": [ "set_enabled" ] } }
  }
}
